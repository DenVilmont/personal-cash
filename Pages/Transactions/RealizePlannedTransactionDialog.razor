@inherits PersonalCash.Shared.UiComponentBase

@using Microsoft.AspNetCore.Components
@using MudBlazor
@using Domain.Enums
@using Domain.Contracts

<MudDialog>
    <DialogContent>
        <MudGrid Class="mt-2">
            <MudItem xs="12" sm="6">
                <MudDatePicker Label=@L["Date"]
                               @bind-Date="OccurredOnPicker"
                               Variant="Variant.Outlined" />
            </MudItem>

            <MudItem xs="12" sm="6">
                <MudSelect Disabled="true" T="EntryType" Label=@L["Type"] @bind-Value="_entryType" Variant="Variant.Outlined">
                    <MudSelectItem Value="EntryType.Income">@EntryType.Income.ToLocalizedString(L)</MudSelectItem>
                    <MudSelectItem Value="EntryType.Outcome">@EntryType.Outcome.ToLocalizedString(L)</MudSelectItem>
                </MudSelect>
            </MudItem>

            <MudItem xs="12" sm="6">
                <MudSelect T="Guid"
                           Label=@L["Account"]
                           @bind-Value="_accountId"
                           Variant="Variant.Outlined"
                           Disabled="true">
                    @foreach (var a in Accounts)
                    {
                        <MudSelectItem Value="@a.Id">
                            <div class="icon-option">
                                <MudIcon Icon="@AccountIconExtensions.FromDbKey(a.IconKey).GetIcon()" />
                                @a.Name (@a.Currency)
                            </div>
                        </MudSelectItem>
                    }
                </MudSelect>
            </MudItem>
            <MudItem xs="12" sm="6">
                <MudNumericField T="decimal"
                                 Label=@L["Amount"]
                                 @bind-Value="_amount"
                                 Variant="Variant.Outlined"
                                 HideSpinButtons="true"
                                 Culture="CultureInfo.CurrentCulture"
                                 Format="0.00"
                                 Adornment="Adornment.Start"
                                 AdornmentText="@_currency" />
            </MudItem>

            <MudItem xs="12" sm="6">
                <MudSelect T="Guid" Label=@L["Category"] @bind-Value="_categoryId" Variant="Variant.Outlined" Disabled="true">
                    @foreach (var c in Categories)
                    {
                        <MudSelectItem T="Guid" Value="@c.Id">@c.Name</MudSelectItem>
                    }
                </MudSelect>
            </MudItem>

            <MudItem xs="12">
                <MudTextField Label=@L["Note"]
                              @bind-Value="_note"
                              Variant="Variant.Outlined"
                              Lines="3" />
            </MudItem>
        </MudGrid>
    </DialogContent>

    <DialogActions>
        <MudButton Variant="Variant.Text" OnClick="Cancel">@L["Cancel"]</MudButton>
        <MudButton Variant="Variant.Filled" Color="Color.Primary" Disabled="_busy" OnClick="SaveAsync">
            @L["Save"]
        </MudButton>
    </DialogActions>
</MudDialog>